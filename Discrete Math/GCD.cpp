// Input: Two positive integers a and b
// Initialize: smaller :=0, gcd:=1 (Note:smaller will in Step 3 be the lesser of a and b)
// If a <= b, then update smaller to equal a; otherwise set smaller equal to b
// For i starting at 1 up through smaller
    // If(a mod i = 0 and b mod i = 0), then update gcd = i
// Output gcd

#include <iostream>
using namespace std;

int main(int argc, char const *argv[]) {
  int a;
  int b;

  for ()
  return 0;
}
